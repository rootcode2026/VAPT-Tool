# Attack Surface

Graph of assets + relationships (domains→subdomains→IPs→services). Change detection via asset_change_events, relationships discovered via cloud/ingestion. Interpret: internet-exposed vs internal, stale vs active.
